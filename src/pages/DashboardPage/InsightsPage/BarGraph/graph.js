import React, { useState} from 'react';
import { Text, TouchableOpacity, View, Button, Alert, FlatList, StatusBar, TouchableHighlight, TextInput, Platform, Modal} from 'react-native';

export default function BarGraph() {
    return (
        <View style = {style.main}>


        </View>
    )
}

const style = StyleSheet.create({
    main: {
        marginVertical: 25,
        marginHorizontal: 0,
    }
});